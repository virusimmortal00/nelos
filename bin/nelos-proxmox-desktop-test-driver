#!/usr/bin/env node
import { runProxmoxDesktopDriverCliV1, writeProxmoxDesktopDriverErrorV1 } from "../src/proxmox-desktop-test-driver.mjs";

runProxmoxDesktopDriverCliV1().catch((error) => { writeProxmoxDesktopDriverErrorV1(process.stdout, error); process.exitCode = 1; });
