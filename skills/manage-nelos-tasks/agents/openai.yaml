interface:
  display_name: "Coordinate Work with Nelos"
  short_description: "Plan parallel work, track dependencies, verify results"
  default_prompt: "Use $manage-nelos-tasks to split this coding work into safe parallel tasks, track dependencies, and verify the results."
  brand_color: "#111111"
